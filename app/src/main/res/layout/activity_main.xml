<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
<!--        //写法一-->
<!--        <variable-->
<!--            name="mTestBean"-->
<!--            type="com.oneday.videodemo.model.TestObservable" />-->
        //写法二
        <import type="com.oneday.videodemo.model.TestObservable"/>
        <variable
            name="mTestObservable"
            type="TestObservable" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <TextView
            android:id="@+id/tvContent"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/w280dp"
            android:gravity="center"
            android:text="@{mTestObservable.mContent + mTestObservable.mTime}"
            android:textSize="@dimen/h24sp"
            android:textColor="@color/colorAccent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btTest"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvContent"
            android:textColor="@color/colorPrimary"
            android:textSize="@dimen/h24sp"
            android:text="@string/main_test_button"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>